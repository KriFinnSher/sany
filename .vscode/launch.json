{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run service",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/cmd/server",
            "envFile": "${workspaceFolder}/.env",
            "cwd": "${workspaceFolder}"
        },
        {
            "type": "go",
            "request": "launch",
            "name": "Launch test",
            "program": "${workspaceFolder}",
            "env": {
                "TERM": "xterm-256color",
                "COLORTERM": "truecolor"
            }
        }
    ]
}